//import { createClient } from "@/utils/supabase/server";


export async function deleteAccount(){
    //const supabase = await createClient();
    
    // # TODO:  insert current uuid to my deleteRequests table 
    //          and set up DB function to then handle 
    //    
    //       auth delete on supabase using admin privelages
    
    return
}